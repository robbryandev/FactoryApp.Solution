@using Microsoft.AspNetCore.Components
@using Factory.Views.Shared
@using Factory.Models

<div class="text-center">
  <a class="btn btn-light" href="/machine">Back</a>
  <br>
  <AutoForm title="Add Machine" actionPath="/machine/add" inputs="@formInputs" />
</div>

@code {
  List<(string, string[])> formInputs = new List<(string, string[])>{
("text", new string[]{"name"})
};
}